<?php defined('BASEPATH') or exit('No direct script access allowed');
/** KLMS public view (uses client layout via ClientsController) */

// // Read ?col=... (e.g. col-md-8 col-md-offset-2) safely
// $CI  = &get_instance();
// $col = $CI->input->get('col', true);
// $col = $col ? preg_replace('/[^a-z0-9\-\_\s]/i', '', $col) : 'col-md-8 col-md-offset-2';

// Page title is passed from controller via $data['title'] = _l('klms_get_course');
?>
<div class="container">
  <div class="row">
    
<h1>hello world</h1>

    </div>
  </div>
</div>
